{
  "name": "vnext-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Next.js on Cloudflare Workers using OpenNext and Edge Runtime",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/index.ts --bundle --format=esm --target=es2022 --outfile=dist-worker.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.10"
  }
}
